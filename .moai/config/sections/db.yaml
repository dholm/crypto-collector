db:
    auto_sync:
        debounce_seconds: 10
        enabled: true
        excluded_patterns:
            - .moai/project/db/**
            - .moai/cache/**
            - '**/*.lock'
        require_user_approval: true
    dir: .moai/project/db
    enabled: true
    engine: PostgreSQL
    migration_patterns:
        - prisma/schema.prisma
        - alembic/versions/**/*.py
        - db/migrate/**/*.rb
        - migrations/**/*.sql
        - supabase/migrations/**/*.sql
        - sql/migrations/**/*.sql
    migration_tool: sqlx migrate
    multi_tenant: none
    orm: sqlx
